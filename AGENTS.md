# AGENTS.md

이 파일의 규칙은 `C:\SellerFind` 아래 전체에 적용됩니다.

## Working Style
- 기존 구조와 문서 흐름을 먼저 파악한 뒤 수정한다.
- 요청 범위를 벗어나는 리팩터링은 하지 않는다.
- 변경은 작고 명확하게 유지한다.
- 설명이나 답변은 한국어로 우선 작성한다.

## Editing Rules
- 기존 파일 스타일과 네이밍을 따른다.
- 불필요한 파일 생성은 피한다.
- 하드코딩보다 설정 가능한 구조를 우선한다.
- 주석은 꼭 필요한 경우에만 짧게 추가한다.

## Commands
- 파일 검색은 가능하면 `rg`를 우선 사용한다.
- 큰 변경 전에는 관련 파일과 문서를 먼저 읽는다.
- 테스트나 실행 명령이 확인되면 그 프로젝트의 기본 명령을 따른다.

## Documentation
- 새로운 규칙이나 작업 흐름을 추가하면 관련 문서도 함께 갱신한다.
- 문서만 있는 디렉터리에서는 구현 가정보다 명시된 요구사항을 우선한다.

## Safety
- 사용자 변경사항은 임의로 되돌리지 않는다.
- 파괴적 명령은 사용자 요청 없이 실행하지 않는다.
- 확신이 낮은 경우에는 가정을 명시하고 진행한다.
